@PACKAGE_INIT@

set_and_check(DtkGui_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")
set(DtkGui_TOOL_DIR "@PACKAGE_TOOL_INSTALL_DIR@")

include(CMakeFindDependencyMacro)
find_dependency(DtkCore REQUIRED)
set(DtkGui_LIBRARIES dtkgui ${DtkCore_LIBRARIES})

include_directories("${DtkGui_INCLUDE_DIR}")

check_required_components(DtkWidget)

# Keep deprecated variables for compatibility
set(DTKGUI_INCLUDE_DIR ${DtkGui_INCLUDE_DIR})
set(DTKGUI_TOOL_DIR ${DtkGui_TOOL_DIR})
